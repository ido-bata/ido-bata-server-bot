node_modules/
dist/
coverage/
.tmp/
.reference/

# Env / secrets — keep committed examples only
.env
.env.*
!.env.example
!.env.*.example

# Logs
*.log

# Local persisted state for the timekeeper feature
data/timekeeper-history.json

# Editor / OS / tooling
.DS_Store
.idea/
.vscode/
*.tsbuildinfo

# Claude Code worktrees — per-worker isolated mutable workspaces.
# eslint / vitest already exclude this path; .gitignore keeps biome in sync.
.claude/worktrees/
