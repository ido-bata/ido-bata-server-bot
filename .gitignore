node_modules/
dist/
coverage/
.tmp/
.reference/

# Env / secrets — keep committed examples only
.env
.env.*
!.env.example
!.env.*.example

# Logs
*.log

# Local persisted state for features
data/timekeeper-history.json
data/timekeeper-sessions.json
data/birthdays.json

# Local runtime config (see data/config.example.json + src/features/config-hot-reload/)
data/config.json

# Local persisted state for the scheduled-announcements feature
data/scheduled-announcements.json

# Local persisted state for the poll feature
data/polls.json

# Local persisted state files (runtime only — these are never committed)
data/reminders.json

# Consent registry — user-keyed grants persisted by ConsentService.
# Subject identifiers and revocation timestamps must never be committed.
data/consent.json

# Local persisted state for the iCal calendar feature
data/calendars.json
data/calendar-cache.json

# Editor / OS / tooling
.DS_Store
.idea/
.vscode/
*.tsbuildinfo